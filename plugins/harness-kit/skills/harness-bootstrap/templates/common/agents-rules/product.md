<!-- HARNESS STARTER KIT · {{PROJECT_NAME}} · 스택 무관 공통 규칙 · 플레이스홀더({{PROJECT_NAME}}·{{PROJECT_SLUG}}·{{PACKAGE_NS}}·{{DOMAIN_EXAMPLE}}) 치환 후 사용 · 아래 빈칸은 프로젝트에 맞게 채운다 -->

# 제품 컨텍스트 — {{PROJECT_NAME}}

이 파일은 **채우기 템플릿**이다. `< >` 안을 실제 내용으로 채우고 작성 가이드는 지운다.
제품 정체성·경계·우선순위 판단의 기준이 되므로, 기능 나열이 아니라 "무엇을 우선하고 무엇을 미루는가"를 담는다.

## 한 줄 정의

**{{PROJECT_NAME}}**는 `<제품이 무엇을 연결/해결하는지 한 문장>`이다.
범위는 `<가장 좁은 핵심 가치 하나>`에 집중한다.

> 작성 가이드: 대상 사용자 · 핵심 가치 · 차별점을 한 문장에. "무엇을 가능하게 하는가".

## 책임 / 경계

`{{PROJECT_NAME}}`의 책임: `<핵심 도메인 나열: 예 Auth · User · Catalog · Order · Notification>`.

- `<직접 하는 것>`까지 담당한다(비즈니스 검증·권한 체크 등).
- `<직접 하지 않고 위임/외부화하는 것>`은 경계 밖이다(예: 외부 시스템이 담당하는 처리, 별도 서비스로 분리된 파이프라인).
- 이 경계를 넘는 책임을 여기에 몰지 않는다.

> `{{DOMAIN_EXAMPLE}}`은 실제 대표 도메인으로 치환한다(예: order · catalog · user · notification).

## 목표 (절대 우선)

> `<사용자가 X를 하고 나서 Y분/단계 이내에 Z(핵심 성공 경험)를 얻는다>` — 측정 가능한 첫 성공 경험 한 줄.

핵심 end-to-end 흐름:

```
<진입/온보딩> → <핵심 리소스 생성> → <설정 완료>
→ <{{DOMAIN_EXAMPLE}} 핵심 액션> → <결과 확인(출력 + 기록)>
```

> 작성 가이드: 첫 가치를 얻기까지의 최단 경로만. 곁가지 기능은 넣지 않는다.

## 제품 원칙 (개발 시 항상 지킬 것)

1. `<도메인 경계: 리소스 소유·접근 주체를 무엇으로 판정하는가 — 프로젝트가 정의>`.
2. 권한은 요청 경계(1차) + 유스케이스 진입(2차) **두 곳에서** 확인한다([`security.md`](./security.md)).
3. Secret·자격증명은 평문 저장 금지(해시 또는 암호화, 발급 시 1회만 원문 반환).
4. `<비동기/부수효과 분리 등 아키텍처 지향>`.
5. **첫 성공 경험을 빠르게 만드는 것이 기능 수보다 우선한다.**

> 작성 가이드: 원칙은 판단 기준이 되어야 한다. 추상적 슬로건 금지.

## 범위 밖 (지금 만들지 말 것)

`<후속 Phase로 미루는 큰 기능들 나열>`. → 모두 후속 Phase(P2/Enterprise)로 분리한다.

## 우선순위 정의

- **P0**: 목표 핵심 흐름에 필수. 목표 달성 전 반드시.
- **P1**: 다음 마일스톤에 필요. 목표 달성 직후 착수.
- **P2**: 확장/Enterprise. 고객 요구 확인 후.

## 핵심 KPI

`<측정 지표 3~5개 + 목표치>`.

> 작성 가이드: 각 KPI는 숫자 목표(시간·성공률·비율)를 포함한다. 예: "첫 핵심 액션까지 소요 시간 < N분", "핵심 작업 성공률 95%+".

<!-- 상세 원본(프로젝트별): docs/<PRD>.md 로 링크 -->
