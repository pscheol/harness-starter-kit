---
name: hx-update
description: 설치된 하네스를 harness-kit 플러그인의 현재 버전으로 올린다. 규칙·스크립트·슬래시 커맨드 변경분을 반영하되, 사용자가 고친 파일은 덮지 않고 새 버전을 .new 로 옆에 둔다(설치 시점 해시를 .agents/harness-kit.lock 에서 대조). 스택·변형·에이전트는 .agents/harness-kit.json 에서 읽는다. "하네스 업데이트", "킷 새 버전 반영", "규칙 최신화", "harness update/upgrade", "플러그인 업데이트 적용" 요청 시 사용.
---

# hx-update — 킷 새 버전 반영

플러그인이 올라가도 **이미 스캐폴딩된 리포는 그대로다**. 이 스킬이 그 격차를 메운다.

## 무엇을 지키나

핵심 계약 하나다 — **사용자가 고친 파일을 덮지 않는다.**

설치할 때 각 파일의 해시를 `.agents/harness-kit.lock` 에 적어 둔다. 업데이트는 지금
리포에 있는 파일의 해시를 그것과 대조해, 같으면 손대지 않은 것으로 보고 조용히 교체하고,
다르면 사람이 고친 것으로 보고 새 버전을 `<경로>.new` 로 옆에 둔다.

## 판정 5종

| 판정 | 뜻 | 처리 |
|---|---|---|
| `new` | 킷에는 있고 리포에 없다(새 규칙 파일 등) | 설치한다 |
| `same` | 내용이 이미 같다 | 건너뛴다 |
| `update` | 리포 파일이 설치 당시 그대로다 | 새 버전으로 교체한다 |
| `conflict` | 리포 파일이 수정됐다 | **덮지 않고** `.new` 로 저장한다 |
| `orphan` | lock 에는 있는데 킷에서 사라졌다 | 보고만 한다(지우지 않는다) |

`ARCHITECTURE.md`·`product.md`·`structure.md` 처럼 **채우라고 준 파일**은 거의 항상
`conflict` 로 잡힌다. 정상이다 — 그 파일들은 사용자 소유다.

## 사용 절차

1. **스킬 위치 확인** — 이 스킬이 로드된 폴더의 절대경로를 `SKILL_DIR` 로 잡는다.
   플러그인이 방금 업데이트됐다면 그 새 버전의 경로여야 한다.
2. **`--dry-run` 먼저** — 무엇이 바뀌는지 보고 사용자에게 요약한다. 이 단계를 건너뛰지 않는다.
   ```bash
   bash "$SKILL_DIR/update.sh" --dry-run <대상_프로젝트_경로>
   ```
3. **요약 보고** — `new`/`update` 는 몇 건인지, `conflict` 는 **어떤 파일인지 이름을 모두** 보여준다.
4. **승인 후 실행** — `--dry-run` 없이 재실행.
5. **충돌 처리** — 각 `.new` 를 원본과 diff 해 무엇이 달라졌는지 설명하고 병합안을 제안한다.
   사용자 확인 없이 `.new` 를 원본에 덮어쓰지 않는다.
   ```bash
   diff <대상>/.agents/rules/product.md <대상>/.agents/rules/product.md.new
   ```
6. **검증** — `bash scripts/verify.sh` 통과를 확인하고 보고한다.
   미치환 토큰도 함께 본다: `grep -rn '{{' . --include='*.md' | grep -vE '_spec-templates/|\{\{\.\.\.\}\}|PRODUCT_SLUG'`

## 인자

| 인자 | 의미 | 없을 때 |
|---|---|---|
| 첫 위치 인자 | 대상 프로젝트 경로 | 현재 작업 디렉터리 |
| `--dry-run` | 판정만 출력 | 실제 반영 |
| `--accept-all` | 수정본도 새 버전으로 덮는다(원본은 `.bak`) | 수정본은 `.new` 로 병기 |

## 주의

- `--accept-all` 은 **사용자가 명시적으로 요구할 때만** 쓴다. 하네스 규칙을 팀 사정에 맞게
  고쳐 둔 리포에서 이걸 돌리면 그 커스터마이징이 `.bak` 으로 밀려난다.
- 업데이트는 **에이전트 구성을 바꾸지 않는다**. 메타의 `agents` 에 있는 것만 대상이다.
  Cursor 를 새로 쓰고 싶다면 `hx-agent-add` 가 맡는다.
- 상태 파일이 없는 리포(구버전 킷으로 설치)는 `exit 1` 로 멈춘다. 안내대로 `setup.sh` 를
  한 번 돌려 상태 파일을 만든 뒤 다시 실행한다. 기존 파일은 skip 되므로 안전하다.
- `orphan` 은 자동으로 지우지 않는다. 킷에서 빠진 파일이라도 프로젝트가 쓰고 있을 수 있다.

## 관련

- 초기 설치: `hx-bootstrap` 스킬
- 에이전트 추가: `hx-agent-add` 스킬
- 파일→경로 맵: `../hx-bootstrap/manifest.md`
