---
name: skill-name
description: Use when (어떤 작업·파일에서 호출할지). Covers (무엇을 다루는지).
---

# Skill Name

## What

(이 스킬이 다루는 범위 한 문단. 어떤 파일·모듈이 관련되는지 경로로 명시한다.)

관련 파일:

- `path/to/owner.ts` — 역할
- `path/to/config.ts` — 역할

## Why

(왜 이 절차가 필요한가. 이 절차를 건너뛰면 무엇이 깨지는가.)

절차를 어겼을 때의 실제 증상을 적는다. 증상이 적혀 있으면 모델이 스스로 검증할 수 있다.

## How

### 1. (첫 단계)

```text
(명령 또는 코드)
```

### 2. (다음 단계)

### 3. 검증

```bash
(이 변경이 맞는지 확인하는 명령)
```

## Examples

### 올바른 예

```text

```

### 잘못된 예

```text

```

## Anti-patterns

- ❌ (흔히 저지르는 실수와 그 결과)
- ❌ (이 도메인에서 특히 위험한 것)

## 체크리스트

- [ ]
- [ ]
