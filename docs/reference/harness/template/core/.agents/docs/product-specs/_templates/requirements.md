# 요구사항 - <작업단위>

| 항목      | 값                                                                        |
| --------- | ------------------------------------------------------------------------- |
| 상태      | draft \| in-review \| approved                                            |
| 출처      | 이슈 ID / PRD § / 백로그 Story ID                                         |
| 우선순위  | P0 \| P1 \| P2                                                            |
| 담당 모듈 | (영향받는 모듈)                                                           |
| 연계      | `product-specs/<작업단위>/design.md`, `product-specs/<작업단위>/tasks.md` |

> 작성 가이드: requirements는 **"무엇을/왜"만** 다룬다. "어떻게"에 해당하는 기술 선택·구조·파일 배치는 design에 둔다. 각 수용 기준은 테스트 가능해야 한다.

## 1. 소개

- **문제**: (현재 어떤 문제가 있는가)
- **목표**: (이 작업이 달성할 결과)
- **사용 흐름에서의 위치**: (전체 흐름 중 어디인가)
- **성공 지표**: (측정 가능한 기준)

## 2. 범위

- **포함 범위**:
- **제외 범위**: (명시적으로 제외하는 것. 후속 작업이 있으면 링크)

## 3. 액터

| 액터 | 설명 | 인증/권한 |
| ---- | ---- | --------- |
|      |      |           |

## 4. 용어

- 용어: 정의
- 도메인 용어와 상태 이름은 `.agents/docs/conventions.md`와 일치시킨다.

## 5. 기능 요구사항

> 각 요구사항은 사용자 이야기, 수용 기준(EARS), 우선순위, 추적 ID를 포함한다.

### R1: <제목> (우선순위: P0, 추적: <ID>)

**사용자 이야기:** `<역할>`로서 `<기능>`을 원한다. 그래서 `<가치>`를 얻는다.

#### 수용 기준 (EARS)

1. WHEN `<이벤트>` THEN THE `<시스템>` SHALL `<검증 가능한 동작>`.
2. IF `<조건/예외>` THEN THE `<시스템>` SHALL `<검증 가능한 동작>`.
3. WHERE `<상황>` THE `<시스템>` SHALL `<검증 가능한 동작>`.
4. WHILE `<상태 지속>` THE `<시스템>` SHALL `<검증 가능한 동작>`.

**비고:** (요청/응답 예시, 경계값, 화면 상태 등 구현자가 알아야 할 구체 정보)

### R2: <제목>

**사용자 이야기:**

#### 수용 기준 (EARS)

1.

## 6. 비기능 요구사항

| 분류   | 요구 | 기준/근거                          |
| ------ | ---- | ---------------------------------- |
| 보안   |      | `.agents/rules/security-policy.md` |
| 안정성 |      | `.agents/docs/RELIABILITY.md`      |
| 성능   |      |                                    |
| 관측성 |      | `.agents/docs/QUALITY_SCORE.md`    |

## 7. 제약 / 가정

- **제약**: (반드시 지켜야 하는 것)
- **가정**: (참이라고 전제하는 것. 틀리면 재검토)

## 8. 의존성

- 선행 작업:
- 외부 시스템:
- 근거 자료: (Source ID, 테스트 리포트, 디자인 문서)

## 9. 미해결 질문

- [ ] (확정이 필요한 질문. **추측하지 말고 여기에 남긴다**)

## 10. 승인 체크리스트

- [ ] 모든 요구사항이 테스트 가능한가
- [ ] 포함/제외 범위가 명확한가
- [ ] 추적 ID와 근거가 있는가
- [ ] 비기능 요구사항이 빠지지 않았는가
- [ ] 미해결 질문이 해소되었거나 design으로 명확히 위임되었는가
